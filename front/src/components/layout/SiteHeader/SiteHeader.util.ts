import { createComponentClasses } from "@/lib/mui/componentClasses";

export const siteHeaderClasses = createComponentClasses("SiteHeader", [
  "root",
  "bar",
  "brand",
  "brandName",
  "nav",
]);
