import { createComponentClasses } from "@/lib/mui/componentClasses";

export const specimenClasses = createComponentClasses("Specimen", [
  "root",
  "name",
  "variants",
  "variant",
  "variantLabel",
  "stage",
]);
