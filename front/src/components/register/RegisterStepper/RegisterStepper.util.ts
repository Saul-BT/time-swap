import { createComponentClasses } from "@/lib/mui/componentClasses";

export const registerStepperClasses = createComponentClasses(
  "RegisterStepper",
  ["root", "item", "circle", "label", "connector"],
);
