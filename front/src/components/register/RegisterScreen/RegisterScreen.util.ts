import { createComponentClasses } from "@/lib/mui/componentClasses";

export const registerScreenClasses = createComponentClasses("RegisterScreen", [
  "root",
  "header",
  "brand",
  "brandName",
  "main",
]);
