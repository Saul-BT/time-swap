import { createComponentClasses } from "@/lib/mui/componentClasses";

export const consequenceRowClasses = createComponentClasses("ConsequenceRow", [
  "root",
  "text",
  "title",
  "body",
  "now",
  "control",
]);
