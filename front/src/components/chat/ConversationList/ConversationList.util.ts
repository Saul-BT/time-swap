import { createComponentClasses } from "@/lib/mui/componentClasses";

export const conversationListClasses = createComponentClasses(
  "ConversationList",
  ["root", "item", "link", "meta", "preview"],
);
