import { createComponentClasses } from "@/lib/mui/componentClasses";

export const signInScreenClasses = createComponentClasses("SignInScreen", [
  "root",
  "split",
  "column",
  "brand",
  "brandName",
  "main",
  "note",
]);
