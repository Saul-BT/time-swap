import { createComponentClasses } from "@/lib/mui/componentClasses";

export const signInPanelClasses = createComponentClasses("SignInPanel", [
  "root",
  "eyebrow",
  "title",
  "body",
  "facts",
  "description",
]);
